export type CustomCardProps = {
  experience: {
    title: string;
    body: string;
    footer: string;
  }[];
};

export type FormContactProps = {
  name: string;
  email: string;
  subject: string;
  message: string;
};
